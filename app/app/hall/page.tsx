import HallEntry from "@/components/HallEntry";

export const metadata = {
  title: "The Hall | The First Bank of Friends",
  description: "Walk any Rare Friend into the bank. Genesis included.",
};

export default function HallPage() {
  return <HallEntry />;
}
